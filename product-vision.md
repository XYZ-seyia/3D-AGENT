# 激光切割拼装统一结构内核 — 产品方案

## 一句话定位

**我们在构建一套面向激光切割拼装的统一结构内核，让生成器、编辑器与 AI 围绕同一套结构表达协同工作。**

---

## 核心洞察

### 生成器、编辑器、AI 不是三个产品

如果把三者拆开，都会很快碰到各自的天花板：

- 只有生成器：用户只能调参数，无法继续创作，依赖官方上新维护。
- 只有编辑器：门槛高，更像小众 CAD，难成为主入口。
- 只有 AI：输出不稳定，用户无法可靠地接住错误。

对激光切割拼装场景来说，真正需要被统一的不是界面，而是底层结构表达。

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   生成器入口  │     │  编辑器视图  │     │   AI 对话框  │
│ 参数化创建结构 │     │ 可视化修改结构 │     │ 自然语言修改结构 │
└──────┬──────┘     └──────┬──────┘     └──────┬──────┘
       │                   │                   │
       └──────────────┬────┴────┬──────────────┘
                      ▼         ▼
          ┌──────────────────────────────┐
          │     统一结构表达（JSON）      │
          │ primitives / connections /   │
          │ overrides / decorations      │
          └──────────────┬───────────────┘
                         │
                         ▼
          ┌──────────────────────────────┐
          │  统一结构内核（panel+joint）  │
          │ macro-models / policies /    │
          │ joint-kernel / renderer      │
          └──────────────┬───────────────┘
                         │
                         ▼
          ┌──────────────────────────────┐
          │      导出与制造闭环           │
          │  DXF / SVG / 切割 / 拼装      │
          └──────────────────────────────┘
```

### 激光切割拼装的本质不是自由建模

这个场景的核心不是"做一个 3D 模型"，而是"表达一个可制造、可装配、可纠错的结构"。

用户真正关心的是：

- 面板如何拆分。
- 边与边如何连接。
- 接合如何自动生成。
- 板厚、kerf、槽深是否正确。
- 结构出错后如何被人快速改正。
- 最终是否能稳定导出并进入切割流程。

因此，产品的底层语义必须是面向制造约束的结构表达，而不是任由 AI 或用户直接操作自由几何。

### 为什么必须统一到底层

统一并不是为了架构好看，而是为了让三种交互方式真正互通：

- 生成器创建的结构，编辑器必须能继续修改。
- 编辑器的手动调整，AI 必须能读懂且不覆盖。
- AI 的结构修改，最终必须仍然能走同一套导出与制造链路。

这也是第一阶段最需要被证明的事情：**不是三种入口都已经成熟，而是三种入口已经围绕同一模型打通。**

---

## 为什么是这个方案

### 用户问题决定了不能只做单点产品

激光切割拼装用户的需求天然分层：

- 小白用户希望"选一个现成结构，调几个参数，直接导出"。
- 进阶用户希望"在现有结构上做一点修改，而不是从零开始建模"。
- 高阶用户希望"能把结构继续拆分、添加组件、调整连接关系"。

如果只做生成器，只能满足第一层；
如果只做编辑器，会把所有用户都推向高门槛；
如果只做 AI，会把结构正确性寄托在模型推理上，风险太高。

因此，最合理的产品形态不是三选一，而是：

- 用生成器作为最容易理解的主入口。
- 用编辑器承接可视化修正与深度创作。
- 用 AI 作为结构化操作的效率加速器。

### 这个场景不能接受"看着对，实际上装不上"

激光切割拼装和一般视觉创作不同，它对制造正确性极度敏感。

- 结构看起来合理，不代表切出来能装。
- 参数能被改，不代表接合仍然成立。
- AI 能给出答案，不代表答案具有制造可行性。

所以这条产品线的核心不是"生成得有多自由"，而是"约束得是否足够可靠"。

这也是为什么 AI 在这里不能直接生成任意几何，而必须被约束在结构元素层。

### 为什么不是继续围绕代码生成路线推进

我们此前积累过基于 OpenSCAD 的生成器与 skills，这帮助我们验证了参数化生成器的需求方向，也积累了大量品类理解和结构经验。

但对于激光切割板材拼装这个场景，长期底座仍应回到更贴近制造约束的结构表达，而不是继续让 Agent 面向代码或通用几何语言工作。代码生成路线可以作为历史经验，但不适合作为统一产品底座。

### 结论

这不是一个"生成器 vs 编辑器 vs AI"的选择题，而是一个"是否建立统一结构底座"的判断题。

我们的答案是：

- 生成器继续存在，而且作为对外主入口。
- 编辑器从第一阶段就存在，但先承担统一模型验证职责。
- AI 从第一阶段接入，但定位为效率加速器，而不是首版 headline。

---

## 竞品对比

|          | MakerCase | Cuttle     | Adam.new | 本产品         |
| -------- | --------- | ---------- | -------- | -------------- |
| 输入方式 | 手选+滑块 | 可视化编辑 | 自然语言 | AI+画布双向    |
| 结构类型 | 仅方盒    | 2D图案     | 3D实体   | 激光切割拼装   |
| 接合处理 | 内置      | 无         | 无       | 自动生成       |
| 社区     | 无        | 有         | 无       | 核心目标       |
| AI       | 无        | 无         | 核心     | 核心           |
| 精度保证 | 高        | N/A        | 低       | 高（元素兜底） |

---

## 路线选择：为什么不走 Agent + CAD

### 另一条路线：Agent 控制传统 CAD

业界趋势：用 AI Agent 通过 MCP 控制 Rhino / FreeCAD / OpenSCAD 等传统 CAD 软件。

### 为什么不适合我们

1. **领域太垂直，通用 CAD 是负担。** 激光切割拼装的本质是"有限平面板 + 离散连接方式 + 严格制造约束"，用不到通用 CAD 90% 的能力，反而需要 CAD 没有的东西（指接榫齿数计算、A/B 互补边分配、kerf 补偿）。
2. **AI 输出空间太大。** Rhino MCP 让 AI 可调用任意 CAD 命令，违背"AI 做选择题不做计算题"的核心原则。出错空间是指数级的。
3. **用户无法接住 AI 错误。** Rhino 里修错需要用户懂 Rhino；我们的编辑器里只需拖滑块或对 AI 说"往上挪一点"。
4. **部署和传播是硬伤。** Rhino 许可证几千美元，FreeCAD 安装门槛高。我们的方案浏览器打开即用，社区飞轮才能转起来。
5. **接合逻辑还是要自己写。** 无论用哪个 CAD 做底座，指接榫/插榫/十字插槽的几何生成都不是 CAD 自带的，仍需自建。

### 结论

自建元素编辑器是正确路线。核心 IP（接合引擎 + 组件编译 + 结构化 AI 约束）不依赖外部 CAD，且已有 ~5000 行成熟实现。

---

## 产品形态

### 近期产品形态：官方生成器目录 + 双视图结构

近期产品形态仍然保留两个视图，但它们在第一阶段承担的职责不同：

1. `使用视图`：对外主入口，承接生成器的参数化使用与导出。
2. `编辑器视图`：统一结构模型的可视化操作面，第一阶段先以灰度/内部能力存在。

首页在近期不定义为开放社区，而定义为**官方生成器目录**：

- 官方盒子生成器
- 官方灯罩生成器
- 官方收纳类结构
- 后续开放的 Remix 入口

### 整体流程

```
┌────────────────────────────────────────────────────┐
│              官方生成器目录（首页）                 │
│ [盒子] [灯罩] [书架] [收纳] ...                    │
└───────────────┬───────────────────────┬────────────┘
                │                       │
                ▼                       ▼
      ┌──────────────────┐    ┌────────────────────┐
      │   使用视图        │    │   编辑器视图        │
      │ 参数调节 + 预览    │    │ 结构修改 + AI 协作   │
      │ 导出切割文件       │    │ override / panel    │
      └────────┬─────────┘    └────────┬───────────┘
               │                        │
               └──────────┬─────────────┘
                          ▼
                ┌──────────────────┐
                │ 统一结构表达 JSON │
                └──────────────────┘
```

### 使用视图

用户从官方目录点击某个生成器后进入，界面以"可用、可导出"为主：

- 中央：3D 结构预览。
- 右侧：参数滑块与材料相关参数。
- 面内容区域：对指定面做轻量 DIY，如图案、开孔、文字或装饰预设。
- 底部：导出按钮。
- 次级入口：进入编辑器 / 开启结构修改。

第一阶段里，使用视图承担的是：

- 证明生成器仍然是最容易理解的入口。
- 让用户完成从创建到导出的最短路径。
- 让消费者用户不进入编辑器，也能完成基础的面图案个性化。
- 为统一模型提供一个稳定的外部创建入口。

### 编辑器视图

编辑器不是另一套产品，而是同一结构系统的深度操作界面。

第一阶段它的定位不是完整创作工具，而是用于验证三件事：

- 生成器创建出来的结构可以继续被修改。
- AI 对结构的修改可以被人工接住。
- 手动修改会以 override 等结构化方式被保留。

编辑器的近期能力重点是：

- 结构查看与调试。
- 基础参数修改。
- 有限结构修改。
- 完整的面内容编辑，如开孔、镂空、图案与装饰排布。
- AI 辅助修改同一结构。

完整画布编辑能力放在后续阶段逐步形成，而不是第一阶段一次做全。

### 两个视图的关系

```
官方生成器目录 ─→ 使用视图 ─→ 编辑器视图 ─→ 导出
       │             │             │
       └─────────────┴─────────────┘
              始终共用同一结构模型
```

近期最重要的不是"两个视图都做得很重"，而是证明两者围绕同一结构表达无缝切换。

### 2D 面编辑是横向共享能力

2D 面编辑不属于某一个页面，而属于统一结构系统中的共享能力层。

- 对消费者用户：它以内嵌能力出现在生成器的使用视图中，支持对指定面做轻量 DIY。
- 对生产者用户：它出现在编辑器中，和结构编辑一起组成完整创作闭环。

这意味着：

- 生成器不只是调尺寸，也承载有限个性化。
- 编辑器不只是"制造生成器的工具"，也能直接完成结构与面内容的完整创作。
- 两边操作最终都回到同一份 `decorations` / 结构模型表达中。

---

## 统一结构内核

### JSON 数据模型是系统的宪法

```json
{
  "primitives": [
    {
      "id": "box_1",
      "primitive": "box",
      "params": { "length": 200, "width": 150, "height": 100, "thickness": 3 },
      "joints": { "type": "finger" }
    },
    {
      "id": "shelf_1",
      "primitive": "shelf",
      "parent": "box_1",
      "params": { "height_ratio": 0.5, "thickness": 3 }
    }
  ],
  "connections": [
    {
      "a": "box_1.front.left",
      "b": "box_1.left.right",
      "joint": "finger"
    }
  ],
  "overrides": {
    "shelf_1": {
      "params": { "height_ratio": 0.45 },
      "note": "人工微调"
    }
  },
  "decorations": {
    "box_1.front": [
      { "type": "circle", "cx": 0, "cy": 20, "radius": 8, "mode": "cut" }
    ]
  },
  "meta": {
    "name": "桌面收纳盒",
    "source": "generator+editor+ai"
  }
}
```

这份 JSON 不是调试输出，而是系统中各入口共享的真实中间层：

- 生成器往里写结构。
- 编辑器围绕它做手动修改。
- AI 只对它做结构化操作。
- 2D 面编辑把图案、开孔、装饰写回同一份 `decorations` 数据。
- 导出链路从它读取结构。

### 元素体系

底层元素不追求一开始覆盖所有结构，而追求稳定表达激光切割拼装的共性：

- `panel`：一块板。
- `connection`：两个边之间的连接关系。

在此之上，再逐步形成高频组件：

- `box`
- `shelf`
- `divider`
- `lid`
- `polygon_tube`
- `cross_slot`

原则是：

**所有高阶结构最终都必须编译到 `panel + connection`。**

### 接合体系

接合（Joint）是系统中与元素、组件并列的第三个核心概念。元素回答"用什么拼"，组件回答"拼成什么"，接合回答"怎么拼"。

| 概念 | 回答什么 | 例子 |
|------|---------|------|
| 元素 | 用什么拼 | panel、connection |
| 组件 | 拼成什么 | box、shelf、lampshade |
| 接合 | 怎么拼 | finger、tab-slot、snap-fit |

每种接合方式通过接合注册表（`joint-registry.js`）正式声明，包含：

- **参数定义**：该接合方式可配置的参数（如 kerf、齿数、卡扣宽度等）。
- **适用约束**：最小/最大板厚、最小边长、兼容材料等。
- **几何生成**：对应的边缘轮廓生成函数。

当前已注册的接合类型：

- `finger`：指接榫 — 交替齿槽咬合，强度高，通用性最好。
- `tab-slot`：插榫 — 间隔式插榫，拆装方便。
- `flat`：平接 — 无接合轮廓，需胶合或外部固定。

后续可注册：

- `snap-fit`：卡扣卡槽 — 弹性卡扣配合，适合反复拆装的亚克力结构。
- `living-hinge`：活页铰链 — 通过密集切缝实现弯折。

原则是：

**新增接合方式必须通过注册表准入。** 在 `joint-registry.js` 中声明参数、约束和几何函数后，Agent、编辑器、组件编译器均可自动发现和使用该接合。不允许在 kernel 里硬编码新接合类型而不注册。

### AI 约束原则

AI 的职责不是自由生成几何，而是对现有结构系统做受约束操作。

```json
{ "action": "add", "primitive": "shelf", "parent": "box_1", "params": { "height_ratio": 0.5 } }
```

也就是说：

- AI 做"选元素、填参数、调用操作"的选择题。
- 结构计算、接合分配、几何展开由系统处理。
- 人工修改通过 override 保留，AI 不应覆盖。

这保证了 AI 只是效率加速层，而不是制造正确性的唯一来源。

### override 的意义

统一模型里最关键的不是"谁创建了结构"，而是"人如何接住系统和 AI 的结果"。

因此，人工修改必须有独立表达层：

- 手动位移
- 局部删除
- 参数微调
- 锁定某些结构

这也是编辑器和 AI 能够连续协作的前提。

---

## 统一核心引擎架构

```
用户画布 ─────┐          ┌───── AI Agent
              │          │
              ▼          ▼
         ModelOps（统一操作层）
         addRectPanel / addConnection / setOverride / movePrimitive
              │
              ▼
         PrimitiveLayer（元素层）
         panel + connection
              │
       ┌──────┼──────┐
       ▼      ▼      ▼
   JointKernel  Policies    Three.js Renderer
   finger/tab   edge typing  ExtrudeGeometry
   cross-slot   kerf policy  mesh + explode
              ▲
              │
         MacroModels（组件层）
         box → 6 panels + 12 connections
         polyhedron → N panels + shared-edge connections
```

### 核心模块清单

| 模块             | 文件                             | 职责                                                  |
| ---------------- | -------------------------------- | ----------------------------------------------------- |
| Schema           | `js/core/schema.js`            | 模型/元素/连接的数据类型定义，AI 可写 vs 系统求解字段 |
| ModelOps         | `js/core/model-ops.js`         | 无状态操作：增删改元素、连接、override、自动连接检测  |
| JointRegistry    | `js/core/joint-registry.js`    | 接合注册表：接合类型定义、参数声明、约束与校验        |
| JointKernel      | `js/core/joint-kernel.js`      | 几何层：按注册表分发接合轮廓生成、面板 Mesh 创建      |
| JointPolicies    | `js/core/joint-policies.js`    | 规则层：边类型分配、kerf 策略、盒子边样式规则         |
| MacroModels      | `js/core/macro-models.js`      | 编译层：box/polyhedron 编译为 panel+connection 组合   |
| AssemblyRenderer | `js/core/assembly-renderer.js` | 渲染层：JSON assembly → THREE.Group，爆炸图控制      |

### Skill 分类

| 类型     | 作用                                     | 数量   |
| -------- | ---------------------------------------- | ------ |
| 编排型   | 告诉 Agent 能执行哪些结构化操作          | 1      |
| 领域知识 | 告诉 Agent 有哪些元素、接合规则、约束    | 2-3    |
| 模型模板 | 高阶快捷方式（box / polyhedron / shelf） | 按需加 |

对 AI 暴露的 skill 总数控制在 3-6 个。底层代码模块按职责拆，但不必都叫 skill。

---

## 生成器工厂策略

### 核心洞察：生成器和编辑器是同一件事

每个新生成器 = 一个新组件 = 编辑器里的一个新元素。两条看似矛盾的需求（"公司要持续上新生成器"和"长期做 AI 编辑器"）有统一解。

```
┌─────────────────────────────────┐
│     生成器页面（薄壳）            │  ← 每个生成器一个，2周交付
│  参数滑块 + 3D 预览 + 导出按钮    │
└──────────────┬──────────────────┘
               │ 调用
               ▼
┌─────────────────────────────────┐
│     组件层（macro-models.js）   │  ← 每个生成器 = 一个编译函数
│  lampshade() / shelf() / box()  │
└──────────────┬──────────────────┘
               │ 编译为
               ▼
┌─────────────────────────────────┐
│     核心引擎（已建好）             │  ← 所有生成器共享
│  panel + connection + joint      │
│  + renderer + export             │
└─────────────────────────────────┘
```

### 示例：灯罩生成器

```
lampshade 组件
├── top_ring    — 顶部环形板（polygon_tube 变体）
├── bottom_ring — 底部环形板
├── rib × N     — N 块肋板，等角圆形阵列
└── connections  — 每块肋板与上下环的十字插槽连接
```

与 box 组件完全同构：`box → 6 panels + 12 connections`，`lampshade → 2 rings + N ribs + 2N connections`。

### 对三方的价值

- **对公司**：每个组件立刻变成独立生成器上线，上新速度从"每月"变为"每两周"
- **对编辑器**：每个生成器都在积累元素，编辑器不是"某天做完上线"，而是随上新自然长出来
- **对 AI**：每个生成器的用户调参数据都是结构化 JSON，自动积累为 AI 训练数据

---

## Remix 链路与生态飞轮

### 核心机制：Remix 是内容的繁殖方式

```
没有 Remix：官方出 1 个灯罩 → 永远是 1 个灯罩
有 Remix：  官方出 1 个灯罩 → 用户 A 改成花瓣灯罩 → 用户 B 加底座变吊灯
            1 → 3 → 9 → ...  内容指数增长
```

每个生成器是种子，Remix 是繁殖机制。官方不再是内容的唯一供给方。

### 三层用户生态

```
                    ▲ 高阶用户（设计师/Maker 大佬）
                   ╱ ╲  从零创作新生成器
                  ╱   ╲ 用编辑器 + AI 组合元素
                 ╱     ╲ 产出：新模板种子
                ╱───────╲
               ╱ 中级用户  ╲
              ╱  有动手能力   ╲
             ╱ Remix 现有生成器 ╲
            ╱ 改参数/加组件/换接合╲
           ╱ 产出：变体模板        ╲
          ╱─────────────────────╲
         ╱       小白用户          ╲
        ╱  浏览 → 选模板 → 调滑块    ╲
       ╱  → 导出 → 切割               ╲
      ╱  产出：使用数据（热门模板/常用参数）╲
     ╱───────────────────────────────╲
```

| 用户层                             | 行为                             | 产出                         | 使用的功能                      |
| ---------------------------------- | -------------------------------- | ---------------------------- | ------------------------------- |
| **高阶用户**（设计师/Maker） | 从零创作，或深度 Remix           | 新模板种子，供他人 Remix     | 编辑器画布 + AI 对话 + 全部元素 |
| **中级用户**（有动手能力）   | Remix 现有生成器：改结构、加组件 | 变体模板，丰富社区内容       | Fork + 参数面板 + AI 对话       |
| **小白用户**（终端消费者）   | 浏览 → 调参 → 导出             | 使用数据：热门模板、常用参数 | 参数滑块 + 3D 预览 + 导出       |

每一层的产出都在喂养上一层和下一层：小白的使用数据告诉中级用户"什么受欢迎"，中级用户的变体给小白更多选择，高阶用户的新种子给中级用户更多 Remix 素材。

### Remix 的两种深度

**浅 Remix：只改参数**

```
官方灯罩（8 根肋骨）→ 用户改成 12 根 → 发布为"12 骨灯罩"
```

- 本质是存了一组参数预设，量大
- 数据价值：告诉我们用户偏好什么参数范围

**深 Remix：改结构**

```
官方灯罩 → 用户加了底板 + 悬挂孔 → 发布为"吊灯版灯罩"
```

- 真正的内容创作，产出新的结构组合
- 最有价值的 AI 训练数据："用户认为灯罩应该加什么"
- UI 应引导用户做深 Remix（Remix 时默认打开"添加组件"面板）

### Remix 在 JSON 层的实现

现有 schema 天然支持，不需要新机制：

```json
{
  "primitives": [
    { "id": "ring_top", "primitive": "ring", "params": { "radius": 120 } },
    { "id": "shelf_1", "primitive": "shelf", "params": { "height_ratio": 0.5 } }
  ],
  "overrides": {
    "ring_top": { "params": { "radius": 150 }, "note": "用户加大了顶环" }
  },
  "meta": {
    "name": "吊灯版灯罩",
    "author": "user_42",
    "fork_from": "official/lampshade",
    "fork_chain": ["official/lampshade"],
    "source": "remix"
  }
}
```

Remix = Fork JSON + 改 overrides + 加 primitives + 写 `fork_from`。

### 三个飞轮的联动

```
        ┌──────────────┐
        │   内容飞轮    │  1 个种子 → N 个社区变体 → 吸引更多用户
        └──────┬───────┘
               │ 社区变体产出结构化 JSON
               ▼
        ┌──────────────┐
        │   AI 飞轮     │  Remix diff = 训练数据 → AI 更懂"用户想怎么改"
        └──────┬───────┘
               │ AI 建议更准，用户更愿意创作
               ▼
        ┌──────────────┐
        │   元素飞轮    │  高频 Remix 模式沉淀为新元素 → 创作更高效
        └──────┬───────┘
               │ 新元素 = 新种子
               ▼
          回到内容飞轮
```

- **内容飞轮**：种子 → 变体 → 更多用户 → 更多 Remix → 更多变体
- **AI 飞轮**：Remix JSON diff → 训练数据 → AI 建议更准 → 更多人用 AI Remix
- **元素飞轮**：大量用户在灯罩里加隔板 → 沉淀为 `lampshade_with_shelf` 组件 → 官方上新

### Remix 的上线节奏

| 能力                  | 所需功能                      | 阶段         |
| --------------------- | ----------------------------- | ------------ |
| Fork 一个生成器       | 复制 JSON，写入 `fork_from` | 第二阶段即可 |
| 改参数并发布          | 已有的参数滑块 + 保存         | 第二阶段即可 |
| 加/删组件（深 Remix） | AI 对话框 或 画布编辑         | 第三阶段     |
| 社区浏览/搜索变体     | 模板列表 + fork_chain 追溯    | 第四阶段     |

Remix 不是一个大功能，而是随编辑器能力增长自然解锁的：第二阶段开放"调参 Remix"，第三阶段开放"结构 Remix"。

---

## 当前基础与后续目标

### 当前已经具备的基础

目前已经形成了一套可用的统一结构内核雏形：

- `schema.js`：模型、元素、连接与 override 的定义。
- `model-ops.js`：增删改元素、连接与自动连接检测。
- `joint-kernel.js`：接合轮廓生成。
- `joint-policies.js`：边类型、kerf 等规则。
- `macro-models.js`：将组件编译为 `panel + connection`。
- `assembly-renderer.js`：将 assembly 渲染为 3D 视图。

已有能力的意义在于：统一模型不是 PPT 设想，而是已经具备可验证的工程基础。

### 当前基础能支撑什么

在近期阶段，这些基础足以支持：

- 盒子、多面体等基础组件。
- 统一结构渲染。
- 基础接合生成。
- 有限的结构化编辑与 override。
- 用 demo 形式验证"生成器 -> 编辑器 -> AI 修改"这条链路。

### 后续阶段要补齐什么

当前还不能把所有愿景写成既成事实。后续阶段重点补齐的是：

- 稳定的 DXF / SVG 导出。
- 材料与板厚的工程化管理。
- 排版与更完整的制造链路能力。
- 轻编辑与基础 Remix。
- 真正可用的画布编辑器。
- 后续的社区与变体发布机制。

因此，技术架构部分必须明确区分：

- 哪些是已经具备基础的能力。
- 哪些是下一阶段要验证和工程化的能力。

---

## 近期不做什么

- 不做通用 CAD。
- 不做任意自由曲面建模。
- 不让 AI 编辑任意复杂 SVG 曲线。
- 不做通用参数化（变量绑定、节点编程）— 参数化由元素自带。
- 不让 AI 处理任意形状 — AI 限制在元素组合范围内。
- 不把社区飞轮当成第一阶段主目标。
- 不把数据反哺 AI 当成近期核心承诺。
- 不以"持续上新生成器数量"作为第一阶段成败判断。

这条产品线的早期重点不是"做得很大"，而是"先把统一结构底座做对"。

---

## 铁律

1. **所有组件必须编译到 panel + connection** — 绕过核心引擎的代码一律打回
2. **Schema 变更只有产品负责人能批准** — 这是宪法
3. **每个生成器必须输出 JSON fixture** — 作为 AI 训练数据自动积累
4. **Remix 产出的 JSON 必须包含 `fork_from` 溯源** — 维护内容链路可追踪

### 给老板的叙事

> 我们不是在做一个长周期的编辑器项目。我们在建一套**生成器工厂**：统一的核心引擎让我们能快速上新各类生成器（灯罩、书架、笔筒...），每个新生成器同时丰富我们的元素库。当元素积累到足够多时，AI 编辑器就是把它们自由组合的入口 — 它不是额外开发的，是自然长出来的。用户 Remix 官方生成器并发布变体，形成"官方种子 → 社区繁殖 → 数据反哺 AI → 沉淀新元素"的增长飞轮。

---

## 实施路径

### 第一阶段：制造闭环 MVP 期

这是第一阶段的对外叙事，但其真正要验证的是统一模型。

#### 对评审会的核心证明

第一阶段最想证明的不是"已经做出了完整编辑器"，而是：

1. 生成器仍然可以作为对外主入口。
2. 同一个结构可以被编辑器继续修改。
3. AI 可以围绕同一结构模型做受约束修改。
4. 修改后的结构仍然进入同一导出与制造链路。

#### 第一阶段要交付的内容

- 生成器主入口与官方目录。
- 统一结构模型与基础组件。
- 使用视图中的参数调节、预览、导出链路。
- 编辑器灰度能力：查看、调试、有限修改。
- AI 有限接入：围绕统一模型做结构化修改。

#### 第一阶段的成功标准

不是"上线了多少新生成器"，而是以下链路是否成立：

**生成器创建结构 -> 编辑器读取并修改 -> AI 继续修改 -> 导出进入制造闭环**

### 第二阶段：轻编辑与制造能力双线增强

在统一模型被证明成立之后，第二阶段才开始增强可用性：

- 更完整的参数暴露机制。
- 更稳定的 DXF / SVG 导出。
- 材料、板厚、kerf 等工程能力增强。
- 轻量 Remix 与基础结构微调。
- 更清晰的"从使用到编辑"的产品流转。

这一阶段的目标不是做重编辑器，而是让"可用"和"可改"都更顺畅。

### 第三阶段：通用编辑能力探索

第三阶段不再写成确定性的完整交付，而写成围绕统一结构模型展开的通用编辑能力探索。

探索方向主要包括：

- 更自然的画布交互方式，如拖拽、放置、选择、局部修改。
- 结构关系的可视化表达，如连接、吸附、约束、层级关系。
- override 的更直观管理方式。
- AI 与可视化编辑协同的交互模式。
- 从"有限修改"走向"更自由但仍受约束的结构编辑"。

这一阶段的重点不是绑定某一个具体品类或功能清单，而是验证：统一结构模型是否足以支撑更通用的编辑体验。

### 第四阶段：通用生态与 Remix 探索

第四阶段也不预设为完整社区上线，而是围绕结构复用与创作传播的通用生态能力进行探索。

探索方向主要包括：

- 结构变体的保存、管理与再次编辑。
- Remix 的基础机制与溯源关系。
- 官方结构与用户变体的共存方式。
- 结构内容的发现、复用与分发逻辑。
- 可能的创作者机制与生态激励方向。

这一阶段关注的不是先搭一个内容广场，而是确认：当统一模型和编辑能力成熟后，是否能够自然长出结构复用与生态传播机制。

---

## 评估标准

### 第一阶段评估标准

- 是否已经形成统一结构模型，而不是各入口各自维护一套数据。
- 生成器、编辑器、AI 是否真的围绕同一模型连续操作。
- 手动修改是否能被结构化保留。
- 导出链路是否足以支撑基本制造闭环。

### 第二阶段评估标准

- 从"使用"到"轻编辑"的链路是否明显顺滑。
- 导出与材料相关能力是否足够稳定。
- 用户是否能做基础结构修改，而不必进入重编辑器。

### 第三阶段评估标准

- 统一结构模型是否能够承接更通用的编辑交互。
- 可视化编辑是否明显降低复杂结构修改门槛。
- 人工编辑与 AI 辅助是否形成互补，而不是互相冲突。

### 第四阶段评估标准

- 结构复用与变体创作是否自然发生。
- 官方目录是否具备向 Remix 机制演进的基础。
- 生态能力是否建立在成熟的结构表达与编辑能力之上。

---

## 风险与边界

### 最大风险不是技术，而是目标过满

如果同时把生成器工厂、重编辑器、AI、社区、数据飞轮全部作为同期目标，项目很容易失焦。

因此需要始终守住优先级：

1. 统一结构表达。
2. 制造闭环。
3. 轻编辑与编辑器增强。
4. AI 辅助提效。
5. 社区与 Remix。

### AI 价值要被放在正确位置

AI 是重要能力，但近期不是这个项目最核心的价值锚点。

它的近期价值主要在于：

- 降低结构修改门槛。
- 提高内部与创作者效率。
- 让用户更容易从生成器进入编辑过程。

不应在近期把以下内容当成主要承诺：

- 数据飞轮会快速反哺模型。
- AI 会成为最强增长引擎。
- AI 能独立完成复杂结构创作。

### 编辑器要循序渐进

第一阶段就承诺完整编辑器，会拉高工程风险，也会稀释统一模型验证的重点。

因此近期编辑器的策略应该是：

- 第一阶段先存在，但不做全。
- 第二阶段增强轻编辑。
- 第三阶段形成真正可用的画布编辑器。

---

## 推进原则

### 产品原则

1. 所有结构能力都围绕统一模型组织。
2. 生成器是主入口，但不是孤立产品。
3. 编辑器不是从零另做，而是围绕统一模型长出来。
4. AI 只做受约束的结构化操作。
5. 所有阶段都必须以制造可落地为前提。

### 研发协作原则

- 核心结构内核保持强约束，避免各业务线绕开统一底座。
- 新能力优先沉淀为结构表达，而不是页面特例。
- 阶段性原型可以快，但统一模型不应失控。
- 不以短期功能上线速度牺牲长期结构一致性。

---

## 团队分工与 AI 时代工作流

### 角色定义

```
产品负责人（你）
├── 定义每个组件的结构分解（核心决策，不可委托）
├── 维护 JSON Schema 和核心引擎的设计规范
├── 用 AI 辅助快速出可运行原型，验证思路后交给研发
└── 审核所有 Schema 变更和组件的结构正确性

研发 A — "引擎线"（要求：能力较强，理解几何逻辑）
├── 维护 js/core/ 核心引擎（schema / model-ops / joint-kernel / joint-policies）
├── 接合算法优化、新接合类型实现
├── 渲染管线、DXF/SVG 导出能力
├── 编辑器画布交互（中后期）
└── 特征：改动少但深，质量要求高，不追求速度

研发 B — "上新线"（要求：执行力强，能照模式快速复制）
├── 按模板编写新组件编译函数（lampshade / shelf / stand...）
├── 每个生成器的参数面板 + 页面
├── 生成器的测试和 JSON fixture 输出
└── 特征：改动多但浅，每个生成器结构同构，大量可用 AI 辅助
```

### 两条线的差异

|              | 引擎线（研发 A）     | 上新线（研发 B）            |
| ------------ | -------------------- | --------------------------- |
| 变更频率     | 低，每月几次         | 高，每周                    |
| 出错成本     | 高（影响所有生成器） | 低（只影响单个生成器）      |
| AI 可替代度  | 低（需理解几何约束） | 高（模式固定，AI 可出初版） |
| 核心能力要求 | 深度思考、几何直觉   | 执行速度、模式复制          |

### AI 时代工作流

**旧流程**（低效，沟通损耗大）：

```
你说需求 → 研发理解需求 → 研发从零写代码 → 你 review → 来回改
瓶颈：研发理解需求 + 从零写代码
```

**新流程**（你出原型，研发工程化）：

**上新线**：

```
Day 1    你：用 AI 写出新组件的编译函数（可运行原型）
Day 2    你：验证 3D 预览，确认参数定义和结构分解
Day 3-7  研发 B：对接引擎 + 参数面板 + 页面 + 测试
         （如需新接合类型 → 提需求给研发 A）
Day 8    上线
```

**引擎线**：

```
你：写清楚接口规范 + 测试用例 + 预期的几何行为
研发 A：实现核心逻辑
你：验收几何正确性
```

**关键转变**：产品负责人的角色从"提需求的人"变为"出原型的人"。研发从"理解需求并实现"变为"在原型基础上工程化"。这直接砍掉最大的沟通损耗。

### 需要守住的纪律

1. **不让研发 A 去做上新的活** — 浪费深度能力
2. **研发 B 不直接改 js/core/** — 所有核心引擎变更走研发 A
3. **每个生成器上线前必须有 JSON fixture 文件** — 自动积累 AI 训练数据
4. **产品负责人是唯一的 Schema 审批人** — 防止数据模型膨胀失控

### 风险提示

最大的风险是产品负责人自己成为瓶颈（因为原型都要你出）。应对措施：

- 前两周优先把第一个生成器的"组件模板"跑通
- 之后研发 B 照模板复制，逐步减少对你的依赖
- 建立组件编写规范文档，降低上手门槛

---

## 已有资产

### 结构与渲染资产

- 盒子生成器能力。
- 多面体生成能力。
- 指接榫、插榫等接合基础。
- 面板装饰与开孔相关能力。
- 统一核心引擎雏形。

### 资产明细

| 资产                              | 状态             | 复用方式                                                                                        |
| --------------------------------- | ---------------- | ----------------------------------------------------------------------------------------------- |
| 盒子生成器 (box-generator.js)     | 已完成           | 重构为 box 组件                                                                               |
| 正多面体生成器 (poly-*.js)        | 已完成           | 重构为 polyhedron 组件                                                                        |
| 指接榫/插榫 (joint-utils.js)      | 已完成           | 统一至 core/joint-kernel.js                                                                     |
| 面编辑器 (face-editor.js)         | 已完成           | 画布中的面板装饰功能                                                                            |
| 装饰引擎 (face-decorations.js)    | 已完成           | 镂空/雕刻功能                                                                                   |
| 声明式 MVP (mvp-box-demo.html)    | 已完成           | JSON 数据模型的验证原型                                                                         |
| SKILL.md / reference.md           | 已完成           | Agent 的领域知识库                                                                              |
| **统一核心引擎 (js/core/)** | **已完成** | **schema + model-ops + joint-kernel + joint-policies + macro-models + assembly-renderer** |

### 历史经验资产

我们此前在 OpenSCAD 与参数化生成器方向上的积累仍然有价值，它们帮助我们验证了：

- 用户确实需要参数化结构生成。
- 某些结构品类值得长期投入。
- 团队已经具备结构拆解与模板抽象经验。

但在这份方案里，这些经验作为背景资产存在，不作为近期主线推进对象。

---

## 在 ATOMM 中的定位

本产品在 ATOMM 中的定位，不是全站通用编辑器，而是一个**面向激光切割拼装场景的专业结构创作内核**。

对平台的价值主要体现在：

- 统一这条垂类中的结构表达与接合逻辑。
- 让生成器、编辑器、AI 使用同一套底层能力。
- 为后续更广泛的结构创作能力提供一个清晰、可验证的样板。

因此，它更适合被定义为：

**ATOMM 在激光切割拼装领域的专业结构创作基础设施。**

### 给 Leader / ATOMM 负责人的汇报口径

1. **不是再做一个盒子生成器。** 我们在做一层统一的结构创作内核，让 generator、AI 和手动画布共享同一套数据模型和接合逻辑。
2. **消除重复建设。** 盒子、多面体、后续书架/隔板都不再各自维护一套几何和开槽代码，全部编译到统一的 panel + connection 底层。
3. **结构化数据沉淀。** 用户在画布上的每一步编辑都产出结构化 JSON，未来可以反哺 AI 能力和模板推荐系统。
4. **AI 做选择题而非计算题。** Agent 只输出结构化的元素操作（选元素、填参数），不做自由空间推理，精度由元素引擎保证。
5. **聚焦垂类，不争顶层。** MVP 只覆盖激光切割拼装这条线，作为 ATOMM Creative Lab 里的一个高价值垂类创作引擎，不试图定义全站产品形态。

### 异形板材策略

异形板材在 MVP 中是"可记录、可引用、有限可操作"的：

- 用户可以创建/导入异形板（`shape.type = 'path'`）
- 系统记录 SVG 路径和可连接边（anchors）
- Agent 可以引用、移动、复制、连接这个板
- Agent 不负责凭空改造复杂曲线

---

## 编辑器交互设计结论

> 详细设计文档见 `docs/interaction-design.md`

### 核心决策

1. **编辑器永远是面板级操作。** 编辑器中不存在"宏"的运行时概念。从模板启动时，先编译为面板再加载。所有操作直接作用于面板。

2. **Figma 式双层选择。** 单击选中整个组件（一组已吸附的面板），双击进入组件内部选中单块板。点击空白退出内部回到整体层级。

3. **吸附即成组，参数自动合并。** 两块面板自动吸附生成卡槽时，自动成为一个"组"。系统识别"值相同的维度"并合并为整体参数（如四块板宽度相同 → 合并为"整体宽度"）。

4. **Override 标记"已自定义"，无硬脱离边界。** 用户在组件内部修改单块板时，修改记录为 override。退出回到整体层级，该参数显示"已自定义"。不存在"脱离 macro"的硬边界——组件始终是组件，只是有些参数被个别板覆盖。

5. **发布弹窗：自动合并建议 + 用户确认 + 实时预览。** 发布为生成器时，系统自动识别可合并参数，用户在预览弹窗中勾选确认、拖动滑块实时验证、调整标签和范围。

### 发布产物数据结构

```json
{
  "template": { "panels": [...], "connections": [...] },
  "bindings": {
    "width": ["front.w", "back.w", "top.w", "bottom.w"],
    "depth": ["left.w", "right.w", "top.h", "bottom.h"],
    "thickness": ["*.thickness"]
  },
  "exposed_params": {
    "width": { "label": "宽度", "min": 50, "max": 500, "default": 200 }
  }
}
```

### 三种入口的统一

```
生成器视图：  macro params → compileMacro() → render         （快速、确定）
编辑器视图：  macro params → compileMacro() → panels → 用户编辑
发布时：      panels → 自动识别绑定 → 用户确认 → 生成器配置
```

---

## 总结

这份方案的重点，不是同时做大生成器、编辑器、AI 和社区，而是先建立一个正确的统一结构底座。

在这个前提下：

- 生成器是主入口。
- 编辑器是统一模型的可视化操作面。
- AI 是结构化操作的效率加速器。
- 制造闭环是第一阶段必须落地的外部交付。
- 社区与 Remix 是后续建立在成熟能力之上的扩展层。

如果第一阶段能够证明"同一结构可以被生成器创建、被编辑器修改、被 AI 续改，并最终进入制造闭环"，那么这条路线就是成立的。
